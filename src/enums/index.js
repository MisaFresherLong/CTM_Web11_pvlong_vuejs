import { FormMode } from "./formMode";
import { NotifyMode } from "./notifyMode";
import { Gender } from "./gender";
import { ToastMessageMode } from "./toastMessageMode";

export const enums = {
  FormMode,
  NotifyMode,
  Gender,
  ToastMessageMode,
};
